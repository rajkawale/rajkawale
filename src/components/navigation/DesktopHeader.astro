---
import LinkedInIcon from '../icons/LinkedInIcon';

const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
  if (path === '/') return currentPath === '/';
  return currentPath.startsWith(path);
};
---

<!-- Desktop Header: Split Layout (Logo Left, Menu Right) -->
<header class="desktop-header hidden md:flex fixed top-0 left-0 w-full z-50 px-8 py-6 justify-between items-center bg-transparent">
  <!-- Left: Brand Logo -->
  <a 
    href="/" 
    id="desktop-logo"
    class="text-4xl font-serif tracking-tight text-white transition-all duration-300 ease-in-out hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.6)] flex items-center"
  >
    <span class="font-light">Raj</span>
    <span class="font-black"> Kawale</span>
  </a>

  <!-- Right: Navigation Menu + Social Icons -->
  <div class="flex items-center gap-6">
    <!-- Navigation Links - Floating Text Style (Hidden on mobile) -->
    <nav class="hidden md:flex items-center gap-8">
      <a 
        href="/#projects" 
        class="nav-link text-base font-medium font-sans transition-all duration-300 ease-in-out text-gray-200 hover:text-white hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.5)]"
      >
        Work
      </a>
      <a 
        href="/blog" 
        class="nav-link text-base font-medium font-sans transition-all duration-300 ease-in-out text-gray-200 hover:text-white hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.5)]"
      >
        Writing
      </a>
      <a 
        href="/notes" 
        class="nav-link text-base font-medium font-sans transition-all duration-300 ease-in-out text-gray-200 hover:text-white hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.5)]"
      >
        Notes
      </a>
      <a 
        href="/contact" 
        class="nav-link text-base font-medium font-sans transition-all duration-300 ease-in-out text-gray-200 hover:text-white hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.5)]"
      >
        Contact
      </a>
    </nav>

    <!-- Social Icons -->
    <div class="flex items-center gap-4 pl-4 border-l border-white/10">
      <a 
        href="https://www.linkedin.com/in/rajkawale/" 
        target="_blank" 
        rel="noopener noreferrer"
        class="text-zinc-400 hover:text-slate-100 transition-colors flex items-center"
        aria-label="LinkedIn"
      >
        <LinkedInIcon client:load className="w-5 h-5" />
      </a>
    </div>
  </div>
</header>


