---
import BaseLayout from '../components/BaseLayout.astro';
import { container } from '../core/di/container';
import BentoGrid from '../components/ui/BentoGrid.astro';
import ProjectCard from '../components/ui/ProjectCard.astro';
import SectionHeader from '../components/ui/SectionHeader.astro';
import ArticleRow from '../components/ui/ArticleRow.astro';
import TrustedBy from '../components/TrustedBy.astro';
import DriveViewer from '../components/ui/DriveViewer.astro';
import type { BlogPost } from '../domains/blog/entities';

import { getCollection } from 'astro:content';

// Get services from DI container
const blogService = container.getBlogService();

// Fetch data
// Fetch data
const allWork = await getCollection('work');
// Get specific featured projects for manual layout
const sinch = allWork.find(p => p.slug === 'sinch');
const raitalk = allWork.find(p => p.slug === 'raitalk');
const madhavFarm = allWork.find(p => p.slug === 'madhav-farm');

// For debug logging
// For debug logging
const projectsToShow = [sinch, raitalk, madhavFarm].filter(Boolean);
console.log('Total work items:', allWork.length);
console.log('Sinch found:', !!sinch);
console.log('RaiTalk found:', !!raitalk);
console.log('MadhavFarm found:', !!madhavFarm);
if (projectsToShow.length === 0) {
  console.error('No featured projects found! Check slugs:', allWork.map(p => p.slug));
}

// Get all blog posts sorted by date (most recent first)
const allBlogPosts = (await blogService.getAllPosts())
  .sort((a, b) => b.publishedAt.getTime() - a.publishedAt.getTime());
const featuredPost = allBlogPosts[0];
const archivePosts = allBlogPosts.slice(1);

/**
 * Calculate reading time from content
 * @param content - HTML content string
 * @returns Reading time in minutes (minimum 1)
 */
function getReadingTime(content: string): number {
  if (!content) return 1;
  // Strip HTML tags
  const textContent = content.replace(/<[^>]*>/g, '');
  // Count words (split by whitespace and filter empty strings)
  const wordCount = textContent.split(/\s+/).filter(word => word.length > 0).length;
  // Calculate reading time (200 words per minute) and round up
  const readingTime = Math.ceil(wordCount / 200);
  // Ensure minimum of 1 minute
  return readingTime < 1 ? 1 : readingTime;
}

// Get reading time for featured post
let featuredPostReadingTime = 1;
if (featuredPost) {
  try {
    const blogData = await import('../data/external/blogger.json');
    const rawPosts = blogData.default || [];
    const rawPost = rawPosts.find((p: any) => p.slug === featuredPost.slug);
    const fullContent = rawPost?.content || '';
    featuredPostReadingTime = getReadingTime(fullContent);
  } catch (e) {
    // Content not available, use default
    console.error('Error loading blog content for reading time:', e);
  }
}

// Get reading times for archive posts
let archivePostsWithReadTime: Array<{ post: BlogPost; readTime: number }> = [];
if (archivePosts.length > 0) {
  try {
    const blogData = await import('../data/external/blogger.json');
    const rawPosts = blogData.default || [];
    archivePostsWithReadTime = archivePosts.map((post) => {
      const rawPost = rawPosts.find((p: any) => p.slug === post.slug);
      const fullContent = rawPost?.content || '';
      const readTime = getReadingTime(fullContent);
      return { post, readTime };
    });
  } catch (e) {
    // If content not available, use default read time of 1
    console.error('Error loading blog content for reading time:', e);
    archivePostsWithReadTime = archivePosts.map((post) => ({ post, readTime: 1 }));
  }
}

// Work Items Data for Selected Business & Product Bets section
const workItems = [
  {
    id: 'raitalk',
    tag: 'AI Native · Zero-to-One',
    title: 'RaiTalk – Cognitive AI for Therapy Discovery',
    description: 'AI system that helps users surface hidden psychological patterns through guided conversation.',
    metrics: [
      'End-to-end product ownership',
      'Prompt architecture & orchestration',
      'Live MVP with real conversations'
    ],
    cta_label: 'Read product narrative →',
    cta_url: '/narrative/raitalk',
    category: 'primary'
  },
  {
    id: 'madhav-farm',
    tag: 'Founder · P&L Ownership',
    title: 'Madhav Farm – Farmer-to-Business Supply Chain',
    description: 'Built and operated a demand-driven supply chain connecting farmers directly with hotels and restaurants.',
    metrics: [
      '₹8L annual revenue',
      '20–25% commission margin',
      '8-member team led'
    ],
    cta_label: 'Read product narrative →',
    cta_url: '/narrative/madhav-farm',
    category: 'primary'
  },
  {
    id: 'unified-cdp',
    tag: 'Enterprise SaaS · Growth',
    title: 'Unified Customer Data Platform',
    description: 'Aligned product strategy with business metrics to drive measurable ROI.',
    metrics: ['+23% ROI delivered'],
    cta_label: 'View summary →',
    cta_url: '/narrative/unified-cdp',
    category: 'secondary'
  }
];

---

<BaseLayout
  title="Raj Kawale | AI & SaaS Product Manager"
  description="AI and SaaS Product Manager with experience across zero-to-one and scaling products, spanning cognitive AI (RaiTalk), B2B SaaS platforms, and consumer systems."
  keywords="AI Product Manager, SaaS Product Manager, zero to one products, scaling tech products, cognitive AI, RaiTalk, B2B SaaS, consumer platforms"
>
  <main class="min-h-screen bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-slate-800 via-slate-900 to-black text-zinc-300 relative z-10" style="padding-top: 150px;">
    <!-- Hero Section -->
    <section id="home" class="relative min-h-[85vh] flex items-center justify-center px-6 md:px-12 py-32 w-full">
      <div class="max-w-[1100px] mx-auto px-6 md:px-12 text-center w-full">
        <!-- Main Headline -->
        <h1 class="text-6xl md:text-7xl font-serif font-bold tracking-tight text-white leading-tight">
          <span class="hero-word hero-think">Think</span><span class="hero-dot hero-dot-think text-gray-500">.</span> <span class="hero-word hero-build">Build</span><span class="hero-dot hero-dot-build text-gray-500">.</span> <span class="hero-word hero-ship">Ship</span><span class="hero-dot hero-dot-ship text-gray-500">.</span>
        </h1>
        
        <!-- Subheading -->
        <h2 class="text-xl font-sans text-gray-400 mt-4">
          Product Manager & Founder
        </h2>
        
        <!-- Divider -->
        <div class="w-12 h-px bg-white/10 mx-auto my-8"></div>
        
        <!-- Feature Card - The "Proof" (RaiTalk) -->
        <a
          href="https://raitalk.rajkawale.com"
          target="_blank"
          rel="noopener noreferrer"
          class="moonlight-container block w-full max-w-md mx-auto bg-white/5 backdrop-blur border border-white/10 rounded-lg px-6 py-8 transition-all duration-300 ease-out cursor-pointer"
          data-track-hero-raitalk
        >
          <p class="text-xs uppercase tracking-widest text-gray-500">
            Currently Building
          </p>
          <h3 class="text-3xl font-bold text-white mt-2">
            RaiTalk
          </h3>
          <p class="text-sm text-gray-300 mt-1">
            Cognitive Emotional AI
          </p>
        </a>
      </div>
    </section>

    <!-- Products & Businesses Built Section -->
    <section id="projects" class="py-24 px-6 md:px-12">
      <div class="max-w-7xl mx-auto">
        <div class="mb-16 text-center">
          <h2 class="text-4xl md:text-5xl font-serif font-bold text-slate-100 mb-4">
            Products & Businesses Built
          </h2>
          <p class="text-zinc-300 text-lg font-sans max-w-3xl mx-auto">
            Ownership across strategy, execution, and outcomes
          </p>
        </div>

        <div class="max-w-[1100px] mx-auto grid grid-cols-1 md:grid-cols-2 gap-6">
          
          <!-- Card 1: RaiTalk (Primary) -->
          <div
            class="group relative rounded-lg overflow-hidden bg-white/[0.02] border border-white/10 hover:border-white/30 hover:shadow-lg hover:shadow-white/5 transition-all duration-200 p-8 min-h-[400px] flex flex-col"
          >
            <div class="flex-1 flex flex-col">
              <span class="inline-block w-fit px-3 py-1 mb-4 text-xs font-mono font-medium text-zinc-400 border border-white/10 rounded">
                AI Native · Zero-to-One
              </span>
              <h3 class="text-2xl md:text-3xl font-serif font-bold text-white mb-4 leading-tight">
                RaiTalk — Cognitive AI for Pattern Awareness & Psychological Insight
              </h3>
              <p class="text-zinc-400 text-base mb-6 leading-relaxed">
                AI system that helps users uncover hidden psychological patterns and gain clarity through guided conversation.
              </p>
              <ul class="space-y-2 mb-6 flex-1">
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>End-to-end product ownership</span>
                </li>
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>Prompt architecture & orchestration</span>
                </li>
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>Live MVP with real conversations</span>
                </li>
              </ul>
            </div>
            <div class="pt-4 border-t border-white/10 flex items-center justify-between gap-4">
              <a
                href="/narrative/raitalk"
                class="text-zinc-400 text-sm font-medium group-hover:text-white transition-colors inline-flex items-center gap-1 hover:gap-2"
              >
                Read details <span class="transition-transform group-hover:translate-x-1">→</span>
              </a>
              <a
                href="https://raitalk.rajkawale.com"
                target="_blank"
                rel="noopener noreferrer"
                class="text-zinc-400 text-sm font-medium hover:text-white transition-colors inline-flex items-center gap-1"
                onclick="event.stopPropagation()"
              >
                View Website <span class="text-xs">↗</span>
              </a>
            </div>
          </div>

          <!-- Card 2: Madhav Farm (Primary) -->
          <div
            class="group relative rounded-lg overflow-hidden bg-white/[0.02] border border-white/10 hover:border-white/30 hover:shadow-lg hover:shadow-white/5 transition-all duration-200 p-8 min-h-[400px] flex flex-col"
          >
            <div class="flex-1 flex flex-col">
              <span class="inline-block w-fit px-3 py-1 mb-4 text-xs font-mono font-medium text-zinc-400 border border-white/10 rounded">
                Founder · P&L Ownership
              </span>
              <h3 class="text-2xl md:text-3xl font-serif font-bold text-white mb-4 leading-tight">
                Madhav Farm – Farmer-to-Business Supply Chain
              </h3>
              <p class="text-zinc-400 text-base mb-6 leading-relaxed">
                Built and operated a demand-driven supply chain connecting farmers directly with hotels and restaurants.
              </p>
              <ul class="space-y-2 mb-6 flex-1">
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>₹8L annual revenue</span>
                </li>
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>20–25% commission margin</span>
                </li>
                <li class="text-zinc-300 text-sm flex items-start">
                  <span class="text-zinc-500 mr-2">•</span>
                  <span>8-member team led</span>
                </li>
              </ul>
            </div>
            <div class="pt-4 border-t border-white/10 flex items-center justify-between gap-4">
              <a
                href="/narrative/madhav-farm"
                class="text-zinc-400 text-sm font-medium group-hover:text-white transition-colors inline-flex items-center gap-1 hover:gap-2"
              >
                Read details <span class="transition-transform group-hover:translate-x-1">→</span>
              </a>
              <a
                href="https://drive.google.com/file/d/1_ptVBOQjw1yIoVTBRpeYCSKeRl4KKL7a/view?usp=drive_link"
                target="_blank"
                rel="noopener noreferrer"
                class="text-zinc-400 text-sm font-medium hover:text-white transition-colors inline-flex items-center gap-1"
                onclick="event.stopPropagation()"
              >
                View Pitch Deck <span class="text-xs">↗</span>
              </a>
            </div>
          </div>

        </div>

        <!-- Card 3: Unified CDP (Secondary - Full Width Below) -->
        <div class="max-w-[1100px] mx-auto mt-6">
          <a
            href="/narrative/unified-cdp"
            class="group relative rounded-lg overflow-hidden bg-white/[0.02] border border-white/10 hover:border-white/30 hover:shadow-lg hover:shadow-white/5 transition-all duration-200 p-8 min-h-[280px] flex flex-col md:flex-row md:items-center justify-between gap-8"
          >
            <div class="flex-1">
              <span class="inline-block w-fit px-3 py-1 mb-4 text-xs font-mono font-medium text-zinc-400 border border-white/10 rounded">
                Enterprise SaaS · Growth
              </span>
              <h3 class="text-xl md:text-2xl font-serif font-bold text-white mb-3 leading-tight">
                Unified Customer Data Platform
              </h3>
              <p class="text-zinc-400 text-base mb-4 leading-relaxed max-w-2xl">
                Aligned product strategy with business metrics to drive measurable ROI.
              </p>
              <div class="text-zinc-300 text-sm">
                <span class="font-medium">+23% ROI delivered</span>
              </div>
            </div>
            <div class="pt-4 md:pt-0 md:border-t-0 border-t border-white/10 md:border-l md:pl-8">
              <span class="text-zinc-400 text-sm font-medium group-hover:text-white transition-colors inline-flex items-center gap-1 group-hover:gap-2">
                View summary <span class="transition-transform group-hover:translate-x-1">→</span>
              </span>
            </div>
          </a>
        </div>

        <!-- View All Work Link -->
        <div class="max-w-[1100px] mx-auto mt-12 text-center">
          <a
            href="/all-work"
            class="text-zinc-400 text-sm font-medium hover:text-white transition-colors inline-flex items-center gap-1 hover:gap-2"
          >
            View All Work <span class="transition-transform hover:translate-x-1">→</span>
          </a>
        </div>
      </div>
    </section>

    <!-- Work Details Modal -->
    <div id="work-details-modal" class="work-modal fixed inset-0 z-50 hidden items-center justify-center p-4" aria-hidden="true">
      <div class="work-modal-backdrop absolute inset-0 bg-black/80 backdrop-blur-sm" data-modal-close></div>
      <div class="work-modal-content relative z-10 max-w-3xl w-full max-h-[90vh] overflow-y-auto bg-[#020617] border border-white/20 rounded-2xl p-8 md:p-12">
        <button
          type="button"
          class="work-modal-close absolute top-6 right-6 w-10 h-10 flex items-center justify-center rounded-full bg-white/5 hover:bg-white/10 border border-white/20 hover:border-white/40 transition-all duration-300 text-white"
          data-modal-close
          aria-label="Close modal"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
        <div id="work-modal-body">
          <!-- Modal content will be injected here -->
        </div>
      </div>
    </div>

    <!-- Section Divider -->
    <div class="section-divider-wrapper flex justify-center">
      <div class="section-divider"></div>
    </div>

    <!-- Trusted By Section -->
    <TrustedBy />

    <!-- Section Divider -->
    <div class="section-divider-wrapper flex justify-center">
      <div class="section-divider"></div>
    </div>

    <!-- Think. Section -->
    <section id="writing" class="section-spacer pb-24 px-6 md:px-12 mt-24">
      <div class="max-w-4xl mx-auto">
        <div class="mb-16 text-center">
          <h2 class="text-4xl md:text-5xl font-serif font-bold text-slate-100 mb-4">
            Think<span class="text-gray-500">.</span>
          </h2>
          <p class="text-zinc-400 text-lg font-sans">
            Making sense of product decisions, business mechanics, and mental models.
          </p>
        </div>

        <style>
          /* Glass Card Hover Effect - Matching Work Section */
          .think-featured-card {
            transition: all 0.3s ease-out;
          }

          .think-featured-card:hover {
            border-color: rgba(255, 255, 255, 0.3);
            box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
          }

          /* Featured Post Meta */
          .featured-post-meta {
            display: flex;
            align-items: center;
            gap: 0.5rem;
          }

          .meta-separator {
            color: rgba(161, 161, 170, 0.4);
            font-size: 0.875rem;
          }

          .meta-read-time {
            font-size: 0.875rem;
            color: #71717a;
          }

          /* Featured Post Title Hover Effect */
          .featured-post-title {
            transition: color 0.2s ease, transform 0.2s ease;
          }

          .think-featured-card:hover .featured-post-title {
            color: #fff;
            transform: translateX(5px);
            text-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
          }
        </style>

        <div class="max-w-4xl mx-auto">
          <!-- Featured Post Card - Full Width Glass Card -->
          {featuredPost ? (
            <a
              href={`/blog/${featuredPost.slug}`}
              class="think-featured-card block w-full bg-white/[0.03] backdrop-blur-xl border border-white/10 rounded-lg p-8 md:p-10 mb-12 transition-all duration-300 ease-out"
              data-track-featured-post
            >
              <div class="flex flex-col md:flex-row gap-6">
                {featuredPost.coverImage && (
                  <div class="featured-post-thumbnail flex-shrink-0">
                    <img
                      src={featuredPost.coverImage}
                      alt={featuredPost.title}
                      class="w-full md:w-48 h-32 md:h-32 object-cover rounded-lg border border-white/10"
                      loading="lazy"
                    />
                  </div>
                )}
                <div class="flex-1">
                  <h3 class="featured-post-title text-2xl md:text-3xl font-serif font-bold text-white mb-4 transition-all duration-300 ease-out">
                    {featuredPost.title}
                  </h3>
                  {featuredPost.excerpt && (
                    <p class="text-base text-zinc-300 mb-4 font-sans line-clamp-2 leading-relaxed">
                      {featuredPost.excerpt}
                    </p>
                  )}
                  <div class="featured-post-meta">
                <time
                  datetime={featuredPost.publishedAt.toISOString()}
                  class="text-sm text-zinc-500 font-sans"
                >
                  {featuredPost.publishedAt.toLocaleDateString('en-US', {
                    month: 'short',
                    day: 'numeric',
                    year: 'numeric',
                  })}
                </time>
                {featuredPostReadingTime > 0 && (
                  <>
                    <span class="meta-separator">·</span>
                    <span class="text-sm text-zinc-500 font-sans meta-read-time">
                      {featuredPostReadingTime} min
                    </span>
                  </>
                )}
                  </div>
                </div>
              </div>
            </a>
          ) : (
            <div class="bg-white/[0.03] backdrop-blur-xl border border-white/10 rounded-lg p-8 mb-12">
              <p class="text-zinc-500 text-sm font-sans">No posts yet.</p>
            </div>
          )}

          <!-- Archive List - Simple Vertical List -->
          {archivePostsWithReadTime.length > 0 && (
            <div class="think-archive-list">
              {archivePostsWithReadTime.slice(0, 5).map(({ post, readTime }) => (
                <ArticleRow post={post} readTime={readTime} />
              ))}
              
              <!-- View Full Archive Link -->
              <div class="archive-link-container">
                <a href="/blog" class="archive-link" data-track-archive-link>
                  View All Archive →
                </a>
              </div>
            </div>
          )}
        </div>
      </div>
    </section>
  </main>
  
  <!-- Drive Viewer Modal -->
  <DriveViewer fileId="" title="Document Viewer" />
</BaseLayout>

<style>
  html {
    scroll-behavior: smooth;
  }

  /* Global Moonlight Glow System */
  .moonlight-text-link {
    color: #888;
    transition: all 0.3s ease-out;
  }

  .moonlight-text-link:hover {
    color: #fff;
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.6);
  }

  .moonlight-container {
    transition: all 0.3s ease-out;
  }

  .moonlight-container:hover {
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
  }

  /* Think. Section Styles */
  .think-featured-card {
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .think-featured-card:hover {
    box-shadow: 0 0 20px rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
    background: rgba(255, 255, 255, 0.05);
  }

  /* Archive Link Container */
  .archive-link-container {
    margin-top: 24px;
    display: flex;
    justify-content: flex-end;
  }

  .archive-link {
    font-family: 'JetBrains Mono', 'Geist Mono', 'Courier New', monospace;
    font-size: 0.875rem;
    color: #888;
    text-decoration: none;
    transition: color 0.2s ease, transform 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
  }

  .archive-link:hover {
    color: #fff;
    transform: translateX(5px);
  }

  /* Work Link Container */
  .work-link-container {
    margin-top: 24px;
    display: flex;
    justify-content: flex-end;
  }

  .work-link {
    font-family: 'JetBrains Mono', 'Geist Mono', 'Courier New', monospace;
    font-size: 0.875rem;
    color: #888;
    text-decoration: none;
    transition: color 0.2s ease, transform 0.2s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
  }

  .work-link:hover {
    color: #fff;
    transform: translateX(5px);
  }

  /* Section Spacing */
  .section-spacer {
    margin-top: 80px; /* Mobile */
  }

  @media (min-width: 768px) {
    .section-spacer {
      margin-top: 120px; /* Tablet/Laptop */
    }
  }

  @media (min-width: 1024px) {
    .section-spacer {
      margin-top: 180px; /* Desktop */
    }
  }

  /* Section Divider */
  .section-divider-wrapper {
    margin: 0 auto;
    padding: 0;
  }

  .section-divider {
    width: 1px;
    height: 60px;
    background: linear-gradient(to bottom, transparent, rgba(255, 255, 255, 0.1), transparent);
  }

  /* Hero Animation - Staggered Fade-in (One-time) */
  .hero-word {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .hero-dot {
    opacity: 0;
    animation: fadeInUp 0.6s ease-out forwards;
  }

  .hero-think,
  .hero-dot-think {
    animation-delay: 0s;
  }

  .hero-build,
  .hero-dot-build {
    animation-delay: 0.5s;
  }

  .hero-ship {
    animation-delay: 1s;
    color: #14b8a6; /* Teal color when it appears */
    animation: fadeInUp 0.6s ease-out 1s forwards, emphasizeShip 0.3s ease-out 1.6s forwards;
  }

  .hero-dot-ship {
    animation-delay: 1s;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes emphasizeShip {
    to {
      color: #ffffff;
      text-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
    }
  }

  /* Work Details Modal Styles */
  .work-modal {
    animation: fadeIn 0.3s ease-out;
  }

  .work-modal-backdrop {
    animation: fadeIn 0.3s ease-out;
  }

  .work-modal-content {
    animation: slideUp 0.3s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .work-modal-close {
    transition: all 0.3s ease-out;
  }

  .work-modal-close:hover {
    box-shadow: 0 0 15px rgba(255, 255, 255, 0.2);
    border-color: rgba(255, 255, 255, 0.5);
  }
</style>

<script>
  // Track Clarity events
  function trackEvent(eventName) {
    if (typeof window !== 'undefined' && window.clarity) {
      window.clarity('event', eventName);
    }
  }

  document.addEventListener('DOMContentLoaded', () => {
    // Track featured post interactions
    const featuredPost = document.querySelector('[data-track-featured-post]');
    if (featuredPost) {
      featuredPost.addEventListener('click', () => {
        trackEvent('Featured_Post_Click');
      });
      
      // Track hover (intent signal)
      let hoverTracked = false;
      featuredPost.addEventListener('mouseenter', () => {
        if (!hoverTracked) {
          trackEvent('Featured_Post_Hover');
          hoverTracked = true;
        }
      });
    }

    // Track hero RaiTalk card click
    const heroRaiTalk = document.querySelector('[data-track-hero-raitalk]');
    if (heroRaiTalk) {
      heroRaiTalk.addEventListener('click', () => {
        trackEvent('Hero_RaiTalk_Click');
      });
    }

    // Track archive link click
    const archiveLink = document.querySelector('[data-track-archive-link]');
    if (archiveLink) {
      archiveLink.addEventListener('click', () => {
        trackEvent('Click_View_Full_Archive');
      });
    }

    // Track View All Work link click
    const viewAllWorkLink = document.querySelector('[data-track-view-all-work]');
    if (viewAllWorkLink) {
      viewAllWorkLink.addEventListener('click', () => {
        trackEvent('Click_View_All_Work');
      });
    }

    // Track scroll past hero
    let heroScrollTracked = false;
    const heroSection = document.getElementById('home');
    if (heroSection) {
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (!entry.isIntersecting && !heroScrollTracked && window.scrollY > 100) {
            trackEvent('Hero_Scroll_Down');
            heroScrollTracked = true;
            observer.disconnect();
          }
        });
      }, { threshold: 0 });
      observer.observe(heroSection);
    }

    // Work Details Modal System
    const modal = document.getElementById('work-details-modal');
    const modalBody = document.getElementById('work-modal-body');
    const modalTriggers = document.querySelectorAll('[data-modal-trigger]');
    const modalCloses = document.querySelectorAll('[data-modal-close]');

    function openModal(trigger) {
      const title = trigger.getAttribute('data-work-title');
      const metric = trigger.getAttribute('data-work-metric');
      const badge = trigger.getAttribute('data-work-badge');
      const description = trigger.getAttribute('data-work-description');
      const tagsJson = trigger.getAttribute('data-work-tags');
      const category = trigger.getAttribute('data-work-category');
      
      if (!title || !metric) return;

      const tags = tagsJson ? JSON.parse(tagsJson) : [];

      modalBody.innerHTML = `
        <div class="space-y-6">
          <div>
            <span class="inline-block px-3 py-1 mb-4 text-xs font-mono font-medium ${
              category === 'enterprise' 
                ? 'text-blue-300 bg-blue-500/10 border border-blue-500/20' 
                : 'text-emerald-300 bg-emerald-500/10 border border-emerald-500/20'
            } rounded-full">
              ${badge || ''}
            </span>
            <h2 class="text-3xl md:text-4xl font-serif font-bold text-white mb-4">
              ${title}
            </h2>
            <div class="mb-6">
              <p class="text-4xl md:text-5xl font-bold ${
                category === 'enterprise'
                  ? 'text-transparent bg-clip-text bg-gradient-to-r from-blue-200 to-white'
                  : 'text-transparent bg-clip-text bg-gradient-to-r from-emerald-200 to-white'
              }">
                ${metric}
              </p>
            </div>
          </div>
          
          <div class="border-t border-white/10 pt-6">
            <h3 class="text-lg font-serif font-bold text-white mb-3">Problem</h3>
            <p class="text-zinc-300 leading-relaxed mb-6">
              ${description || 'Details coming soon...'}
            </p>
          </div>

          <div class="border-t border-white/10 pt-6">
            <h3 class="text-lg font-serif font-bold text-white mb-3">Solution</h3>
            <p class="text-zinc-300 leading-relaxed mb-6">
              ${description || 'Details coming soon...'}
            </p>
          </div>

          <div class="border-t border-white/10 pt-6">
            <h3 class="text-lg font-serif font-bold text-white mb-3">Impact</h3>
            <p class="text-zinc-300 leading-relaxed">
              ${metric}
            </p>
          </div>

          ${tags && tags.length > 0 ? `
            <div class="border-t border-white/10 pt-6">
              <h3 class="text-lg font-serif font-bold text-white mb-3">Tech Stack</h3>
              <div class="flex flex-wrap gap-2">
                ${tags.map(tag => `
                  <span class="px-3 py-1 text-xs font-mono font-medium bg-zinc-950/50 border border-zinc-800/50 rounded text-zinc-300">
                    ${tag}
                  </span>
                `).join('')}
              </div>
            </div>
          ` : ''}
        </div>
      `;

      modal.classList.remove('hidden');
      modal.classList.add('flex');
      document.body.style.overflow = 'hidden';
      const slug = trigger.getAttribute('data-modal-trigger');
      trackEvent(`Work_Modal_Open_${slug}`);
    }

    function closeModal() {
      modal.classList.add('hidden');
      modal.classList.remove('flex');
      document.body.style.overflow = '';
    }

    // Attach event listeners
    modalTriggers.forEach(trigger => {
      trigger.addEventListener('click', () => {
        openModal(trigger);
      });
    });

    modalCloses.forEach(close => {
      close.addEventListener('click', closeModal);
    });

    // Close on Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !modal.classList.contains('hidden')) {
        closeModal();
      }
    });
  });
</script>
